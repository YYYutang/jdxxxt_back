<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.server.mapper.VideoMapper">
    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.example.server.pojo.Video">
        <id column="id" property="id" />
        <result column="url" property="url" />
        <result column="title" property="title" />
        <result column="chapter" property="chapter" />
        <result column="section" property="section" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, url,title,chapter,section
    </sql>
    <select id="findvideo" resultType="Video">
        select id, url,title,chapter,section from video
    </select>
</mapper>
